{
  "name": "socially-approved-carousel-backend",
  "version": "1.0.0",
  "description": "Dummy API for the Socially Approved video carousel task",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "dev": "node --watch --watch-path=src --watch-path=data app.js",
    "test": "vitest run --globals"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "express-rate-limit": "^8.5.2"
  },
  "devDependencies": {
    "supertest": "^7.2.2",
    "vitest": "^4.1.10"
  }
}
